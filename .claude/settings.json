{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // \"\"'); echo \"$f\" | grep -qE '/frontend/.*\\.(tsx?|jsx?|css|mjs)$' || exit 0; cd /Users/sanket/Desktop/leetnode/frontend && npm run build 2>&1 | tail -30; [ $? -eq 0 ] || exit 2",
            "timeout": 120,
            "statusMessage": "Building frontend...",
            "asyncRewake": true,
            "rewakeMessage": "Frontend build failed after your last edit:"
          }
        ]
      }
    ]
  }
}
